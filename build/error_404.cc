//this file is generated by program(drogon_ctl) automatically,don't modify it!
#include "error_404.h"
#include <drogon/utils/OStringStream.h>
#include <string>
#include <map>
#include <vector>
#include <set>
#include <iostream>
#include <unordered_map>
#include <unordered_set>
#include <algorithm>
#include <list>
#include <deque>
#include <queue>
using namespace drogon;
std::string error_404::genText(const DrTemplateData& error_404_view_data)
{
	drogon::OStringStream error_404_tmp_stream;
	std::string layoutName{""};
	error_404_tmp_stream << "The page that you are looking for cannot be found. :-(\n";
if(layoutName.empty())
{
std::string ret{std::move(error_404_tmp_stream.str())};
return ret;
}else
{
auto templ = DrTemplateBase::newTemplate(layoutName);
if(!templ) return "";
HttpViewData data = error_404_view_data;
auto str = std::move(error_404_tmp_stream.str());
if(!str.empty() && str[str.length()-1] == '\n') str.resize(str.length()-1);
data[""] = std::move(str);
return templ->genText(data);
}
}
