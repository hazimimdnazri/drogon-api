//this file is generated by program(drogon_ctl) automatically,don't modify it!
#include "Layout.h"
#include <drogon/utils/OStringStream.h>
#include <string>
#include <map>
#include <vector>
#include <set>
#include <iostream>
#include <unordered_map>
#include <unordered_set>
#include <algorithm>
#include <list>
#include <deque>
#include <queue>
using namespace drogon;
std::string Layout::genText(const DrTemplateData& Layout_view_data)
{
	drogon::OStringStream Layout_tmp_stream;
	std::string layoutName{""};
	Layout_tmp_stream << "Topbar layout\n";
{
    auto & val=Layout_view_data[""];
    if(val.type()==typeid(const char *)){
        Layout_tmp_stream<<*(std::any_cast<const char *>(&val));
    }else if(val.type()==typeid(std::string)||val.type()==typeid(const std::string)){
        Layout_tmp_stream<<*(std::any_cast<const std::string>(&val));
    }
}
Layout_tmp_stream<<"\n";
	Layout_tmp_stream << "Footer layout\n";
if(layoutName.empty())
{
std::string ret{std::move(Layout_tmp_stream.str())};
return ret;
}else
{
auto templ = DrTemplateBase::newTemplate(layoutName);
if(!templ) return "";
HttpViewData data = Layout_view_data;
auto str = std::move(Layout_tmp_stream.str());
if(!str.empty() && str[str.length()-1] == '\n') str.resize(str.length()-1);
data[""] = std::move(str);
return templ->genText(data);
}
}
